// import axios from "axios"

// const API = axios.create({baseURL: "http://localhost:5000/api/game"})

// //Game CRUD
// export const addToCount = (oneMoreCount) => API.post('/', oneMoreCount)
// export const readCount = () => API.get('/')
// export const updateCount = (id, updatedCount) => API.patch(`/${id}`, updatedCount)
// export const resetCount = (id) => API.delete(`/${id}`)