import React from 'react';

function UserBadges () {
 
    return (
    <div>

    </div>  
        )
}

/*
  function displaybadges(i)  
    loop through userbadgesArray length i++
     opArray = checkifhavebadge(i)
      haveBadge =  opArray[2]
      if(haveBadge = true) 
         return 
         <userbadges num1 = {opArray[0]} num2 = {opArray[1]} /> 
 */

export default UserBadges;
